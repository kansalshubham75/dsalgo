#include <bits/stdc++.h>
using namespace std;


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--){
        // string s;
        // cin>>s;
        // string ls=s.substr(1)+s[0];
        // string rs=s[s.size()-1]+s.substr(0,s.size()-1);
        // vector<vector<int>> dp(s.size()+1,vector<int>(s.size()+1,0));
        // for(int i=s.size()-1;i>=0;i--){
        //     for(int j=s.size()-1;j>=0;j--){
        //         if(ls[i]==rs[j])
        //             dp[i][j]=dp[i+1][j+1]+1;
        //         else
        //             dp[i][j]=max(dp[i+1][j],dp[i][j+1]);
        //     }
        // }
        // cout<<s.size()-dp[0][0];
    }
    return 0;
}