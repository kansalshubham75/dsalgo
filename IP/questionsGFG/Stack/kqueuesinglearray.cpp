#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int n=10;
int k=3;
int f=0;        //keeps track of next free space in st
vector<int> st(n);
vector<int> top(k,-1);
vector<int> e(k,-1);        //holds end of q
vector<int> nidx(n);

int main(){
    
    return 0;
}